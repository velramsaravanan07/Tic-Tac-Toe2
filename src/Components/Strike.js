import React from 'react';

export default function Strike({ strikeClass }) {
  return (
    <div className={`strike ${strikeClass}`}>
      {/* The content of your Strike component */}
    </div>
  );
}
